# ZORA Portfolio

React + Vite 单页个人作品集基础版。页面覆盖全屏 Hero、个人经历、精选项目、个人优势和整屏联系页。

## 启动

```bash
npm install
npm run dev
```

## 优先替换的内容

- `src/main.jsx` 中的姓名、邮箱、社媒、项目标题及数据
- Hero `<video>` 的 `source` 地址，替换为自己的 MP4
- 三个项目卡片（当前为 CSS 生成的艺术化视觉），替换为真实项目图片

版心已控制在 `1700px`，以 PC 端大屏展示为主，并包含基础移动端适配。
